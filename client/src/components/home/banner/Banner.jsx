import React from 'react'

export default function Banner() {

    const styles = {
        wrapper: 'w-full h-[100vh]',
        container: 'w-full h-full',
    }

  return (
    <section id='banner' className={styles.wrapper}>
        <div className={styles.container}>
            <h1>d</h1>
        </div>
    </section>
  )
}
